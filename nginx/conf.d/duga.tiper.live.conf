# C:\project\my_web_project\nginx\conf.d\duga.tiper.live.conf

# HTTP (ポート80) の設定 - Certbot認証中はHTTPSリダイレクトを一時無効化
server {
    listen 80;
    server_name duga.tiper.live;

    # CertbotのチャレンジファイルをHTTPで直接提供するための設定 - ★ここを最優先で配置
    location /.well-known/acme-challenge/ {
        root /var/www/html/app/public; # Certbotがファイルを置くコンテナ内のパス
        try_files $uri =404;
    }

    # return 301 https://$host$request_uri; # ★この行をコメントアウト (Certbot認証中のみ)

    # Certbot認証中は、HTTPで通常のコンテンツをサーブする
    root /var/www/html/app/public; # アプリケーションのpublicディレクトリを指す
    index duga_products.php index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /duga_products.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass php:9000;
        fastcgi_index duga_products.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~* \.(css|js|gif|jpe?g|png)$ {
        expires 1M;
        add_header Cache-Control "public, no-transform";
    }
}

# HTTPS (ポート443) の設定 - ★証明書取得までは引き続きコメントアウト
# server {
#     listen 443 ssl; 
#     http2 on;        
#     server_name duga.tiper.live;

#     ssl_certificate /etc/letsencrypt/live/tiper.live/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tiper.live/privkey.pem;

#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA256:ECDHE-ECDSA-AES256-SHA384:ECDHE-RSA-AES256-SHA384:DHE-RSA-AES128-SHA256:DHE-RSA-AES256-SHA256";
#     ssl_prefer_server_ciphers on;
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";

#     root /var/www/html/app/public; # アプリケーションのpublicディレクトリを指す
#     index duga_products.php index.php index.html index.htm;

#     location /.well-known/acme-challenge/ {
#         root /var/www/html/app/public;
#         try_files $uri =404;
#     }

#     location / {
#         try_files $uri $uri/ /duga_products.php?$query_string;
#     }

#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php:9000;
#         fastcgi_index duga_products.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }

#     location ~* \.(css|js|gif|jpe?g|png)$ {
#         expires 1M;
#         add_header Cache-Control "public, no-transform";
#     }
# }
